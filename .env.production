# Production Environment Configuration for Docker Deployment
# This file shows the required environment variables
# Set these values in Portainer's environment variables section

# Application
NODE_ENV=production
PORT=6363

# MongoDB Cloud Connection
# Replace with your actual MongoDB Atlas or cloud connection string
MONGODB_CONNECTION_STRING=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# CORS Configuration
ALLOW_ORIGIN=*

# JWT Configuration (REQUIRED - set your own secret)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# Session Configuration (REQUIRED - set your own secret)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-min-32-chars

# Google OAuth Configuration (REQUIRED for Google login)
GOOGLE_CLIENTID=your-google-oauth-client-id
GOOGLE_CLIENTSECRET=your-google-oauth-client-secret
GOOGLE_CALLBACKURL=http://your-domain.com:6363/auth/google/callback

# AWS S3 Configuration (for media storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
S3_BUCKET_NAME=your-s3-bucket-name

# Email Configuration (AWS SES)
AWS_SES_REGION=us-east-1
FROM_EMAIL=noreply@yourdomain.com

# AI Services Configuration
OLLAMA_TRANSCRIBE_URL=https://ollama.havenify.ai/transcribe
OLLAMA_API_URL=https://ollama2.havenify.ai/api/generate

# Razorpay Configuration (for payments)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret

# Rate Limiting
RATE_LIMIT_MAX_REQUESTS=100
RATE_LIMIT_WINDOW_MS=900000

# Security
BCRYPT_SALT_ROUNDS=12
